# AI 苏格拉底阅读器 - 功能需求规格说明书 (SRS)

| 文档版本 | V2.8 (修订稿) |
| :--- | :--- |
| 状态 | 修订中 |
| 目标 | 让用户在打开一本书的瞬间，AI 即开始主动引导思考 |

---

## 修订记录

| 版本 | 日期 | 修订内容 |
| :--- | :--- | :--- |
| V2.8 | 2026-04-30 | 补充验收标准、技术限制说明、缺失功能（标注/搜索）、错误处理机制、优先级体系修正、未来扩展性设计指南、修复隐私条款逻辑矛盾、修正角色切换与品牌一致性问题 |
| V2.8.1 | 2026-04-30 | 根据实际实现调整F-008发问逻辑（1-3问题簇）、F-001-B微评类型（3种，第4种列入V2.9）、F-007联网预热（已集成web-search）、F-026/F-027标注与搜索（列入V2.9规划），新增功能实现状态对照表 |

---

## 1. 产品概述

一款以 **AI 主动提问** 为核心的阅读工具。它颠覆了"先读后问"的传统模式，在读者翻开书本前就开始进行思维热身，并在整个阅读过程中通过多角色、多模式的对话，将阅读从被动接收转化为深度思辨。

**核心理念**：打开书，思考就已开始。

**品牌一致性说明**：本产品以"苏格拉底式对话"为默认核心模式。其他角色/模式为可选扩展，不代表产品偏离苏格拉底精神，而是为用户提供多元化的思考角度。所有角色均须遵循苏格拉底式对话的基本原则（追问、引导、不直接给答案）。

---

## 2. 核心功能模块

### 2.1 书籍管理（含 AI 微评引擎）

| 功能ID | 功能名称 | 优先级 | 详细描述 | 验收标准 |
| :--- | :--- | :--- | :--- | :--- |
| F-001 | 导入书籍 | P0 | 支持上传本地 **EPUB**、**PDF**、**TXT**。自动解析元数据（书名、作者、简介），以网格/列表形式展示书架。 | 1. 支持三种格式上传<br>2. 元数据解析成功率 ≥ 95%<br>3. 书架渲染时间 ≤ 1s |
| F-001-A | AI 微评生成 | P1 | 当用户导入一本书后，AI 在后台自动对该书进行快速"预读分析"，并生成一个**个性化微评卡片**，悬挂在该书的封面旁。**若用户未配置 API Key，微评功能入口自动隐藏**。 | 1. 导入完成后 30s 内生成微评<br>2. API 不可用时有优雅降级 |
| F-001-B | 微评内容类型 | P1 | 用户可在设置中选择微评的生成倾向：<br> • **钩子式**（默认）：提炼书中最颠覆认知的一个事实或观点。<br> • **问题式**：提出一个让用户忍不住想翻书寻找答案的尖锐问题。<br> • **共鸣式**：结合用户设定的"用户画像"，生成一句直击个人经历或困惑的见解。<br> • **作者轶事式**（V2.9规划）：抓取作者在创作该书时的一段有趣背景。 | 三种类型均可正常生成，内容无明显幻觉；第4种类型列入V2.9路线图 |
| F-001-C | 微评刷新与固定 | P1 | 每本书的微评卡片支持**手动刷新**。用户也可将某条特别满意的微评**固定置顶**。 | 刷新后新微评覆盖旧微评；固定微评在刷新时不被覆盖 |
| F-001-D | 书架展示形式 | P1 | 支持**画廊视图**（封面大图 + 微评以"书腰"形式叠加）和**列表视图**（左侧小封面 + 书名、作者、进度、微评）。 | 两种视图切换流畅，无布局错乱 |
| F-001-E | 微评可见性 | P1 | 微评卡片旁有"小眼睛"图标，可快速**隐藏/显示**所有微评。 | 设置实时生效，无需刷新页面 |
| F-001-F | 生成时机与性能 | P1 | AI 微评在书籍导入成功后，由独立的轻量级 AI 调用在后台**异步生成**，不阻塞书架刷新。若失败则显示占位文本。 | 导入到书架可交互的时间 ≤ 3s；微评异步生成不阻塞主线程 |
| F-002 | 书架管理 | P1 | 支持按最后阅读时间、进度排序。长按/右键可删除书籍及其关联的全部数据。**删除前弹出确认弹窗**。 | 排序切换实时生效；删除后有撤销确认弹窗 |

> **⚠️ 技术限制说明（F-001）**：AI 微评生成依赖用户配置的 API Key 及对应大模型服务。若用户未配置 API Key，微评功能自动隐藏，不在 UI 中展示入口。若 API 调用失败（超时、限流、余额不足），微评卡片显示优雅降级提示，不影响书架其他功能使用。

---

### 2.2 章节解析

| 功能ID | 功能名称 | 优先级 | 详细描述 | 验收标准 |
| :--- | :--- | :--- | :--- | :--- |
| F-003 | 目录解析 | P0 | 自动解析 EPUB/PDF 的目录结构，以树形或嵌套列表形式展示。 | EPUB 目录解析成功率 ≥ 98%；PDF 目录解析成功率 ≥ 80% |
| F-004 | 章节纯文本提取 | P0 | 用户选择章节后，精准提取该章节全部纯文本，作为 AI 对话核心上下文。**提取前自动检测文件类型和质量**。 | 提取准确率：EPUB ≥ 98%，文本型 PDF ≥ 90%；扫描版 PDF 提示用户 |

> **⚠️ 技术限制说明（F-004）**：
> - **EPUB**：通常可精准提取，但复杂排版（表格、代码块）可能丢失格式。
> - **文本型 PDF**：依赖 PDF.js 提取，表格、多栏排版、数学公式可能提取混乱。
> - **扫描版 PDF**：无法直接提取文本，需调用 OCR（可选功能，P2）。若检测到扫描版，提示用户"当前章节为扫描版，建议手动输入关键段落或启用 OCR（若已配置）"。
> - 提取结果在送入 AI 前，应进行**长度检查**：单章节文本超过 50,000 字时，提示用户"本章节内容较长，AI 将基于前 X 字进行对话，建议分多次阅读"。

---

### 2.3 知识范围配置

| 功能ID | 功能名称 | 优先级 | 详细描述 | 验收标准 |
| :--- | :--- | :--- | :--- | :--- |
| F-005 | 全局上下文设定 | P1 | 新建对话或开新章时，提供可视化选项，定义 AI 回答的"视野"：<br> • **◉ 当前章节**（默认）<br> • **◎ 全书概要**：自动提取简介、核心章节摘要<br> • **Ⓣ 作者背景**：输入作者名，AI 联网检索其生平、风格、代表作<br> • **📎 自定义知识库**：上传/粘贴补充资料（作者访谈、书评等） | 切换视野后，AI 首条回复中明确说明当前视野范围 |

---

### 2.4 读前思维预热（含联网预热）

| 功能ID | 功能名称 | 优先级 | 详细描述 | 验收标准 |
| :--- | :--- | :--- | :--- | :--- |
| F-006 | 打开即问 | P0 | 双击书籍后，AI 在后台自动解析元数据，主动输出第一条预热消息。**用户可在设置中关闭此行为**，改为直接进入阅读界面。 | 首次打开书籍时展示预热；设置关闭后直接进阅读器 |
| F-007 | 联网预热 | P1 | 在设置中开启后，AI 通过**合规方式**（优先使用官方 API，其次使用公开 RSS/摘要接口）检索关于该书的**高质量短评、读书笔记、热门问答**，并融入预热提示词。**当前版本已集成web-search服务，支持搜索引擎检索**。**若用户未配置搜索API或未开启此功能，静默降级为纯本地预热**。 | 联网检索失败时静默降级（不使用联网内容），不弹错；搜索结果明确标注来源 |
| F-008 | 预热阶段一：开放性发问 | P0 | AI 首先发送**1-3条**引导性问题，形成"问题簇"以激发多维度思考。**若用户回复简短(<20字)，AI可追加补充问题；若用户回复深入，直接进入联结引导**。 | 首条消息含1-3个问题；总问题数量根据对话深度动态调整 |
| F-009 | 预热阶段二：联结引导 | P0 | 用户回答后，AI 将用户观点与书中即将阅读的内容进行**试探性联结**。 | 联结内容明确引用用户刚才的回答 |
| F-010 | 预热阶段三：正式过渡 | P0 | 用户输入 `/开始阅读` 或点击按钮后，AI 将预热对话摘要注入上下文，无缝切换到章节精读模式。 | 过渡语句自然，不显突兀 |
| F-011 | 跳过预热 | P1 | 支持 `/跳过预热` 命令，直接进入精读。**设置中可将"跳过预热"设为默认行为**。 | 命令识别准确，过渡无缝 |

> **🔄 设计修正（F-008）**：原设计中"根据复杂度动态决定批量或逐个发送"经过实践验证，**一次性发送1-3个问题形成的"问题簇"能更好地激发用户多维度思考**，且不会造成认知过载。修订为：AI根据书籍复杂度和预热范围，智能生成1-3个关联性问题，用户可选择回答全部或部分问题。

> **⚠️ 合规说明（F-007）**：联网预热的数据获取方式必须在产品说明中透明告知用户。若使用网页抓取，需遵守 `robots.txt` 及目标网站的服务条款。建议在设置面板中提供"数据来源说明"链接。

---

### 2.5 AI 精读对话

| 功能ID | 功能名称 | 优先级 | 详细描述 | 验收标准 |
| :--- | :--- | :--- | :--- | :--- |
| F-012 | 自动生成首问 | P0 | 选章即触发，AI 将章节文本与预热摘要组合，生成首个精读问题。 | 首问生成时间 ≤ 5s（含网络延迟） |
| F-013 | 评价与追问 | P0 | 用户回答后，AI 必须**先点评**（鼓励+指出深化方向），然后基于点评结果提出**更深入的跟进问题**。 | AI 回复中必须包含"评价"和"追问"两个明确部分 |
| F-014 | 自由问答 | P0 | 任何时间可直接向 AI 提问，AI **严格基于当前章节上下文** 作答。若问题超出上下文范围，AI 应诚实说明"该问题超出当前章节范围，建议切换知识视野"。 | 超出范围时 AI 不编造答案 |
| F-015 | 流式响应 | P0 | AI 所有回答都以**打字机效果**逐字输出。 | 流式输出首字延迟 ≤ 1s，后续 token 间隔 ≤ 100ms |
| F-016 | 引用原文 | P1 | 支持 `/引用` 命令或选中文字后，让 AI 引用章节原文并加以分析。 | 引用内容精确匹配原文，误差 ≤ 5 字 |

> **⚠️ 错误处理（AI 相关功能通用）**：
> - **API 调用超时**（> 30s）：提示用户"思考时间较长，请稍后重试"，并提供 `/重试` 命令。
> - **API 返回内容异常**（空回复、格式错误）：自动触发 `/重试`，若连续失败 3 次，提示用户检查 API Key 配置。
> - **上下文过长**（超过模型窗口）：自动截断最早的历史消息，保留最近 10 轮对话 + 当前章节全文。
> - **API Key 无效或余额不足**：在 AI 回复区域显示红色提示条，引导用户前往设置页更新 Key。

---

### 2.6 实时指令系统（含动态角色发现）

| 功能ID | 功能名称 | 优先级 | 详细描述 | 验收标准 |
| :--- | :--- | :--- | :--- | :--- |
| F-017 | 角色切换 | P1 | `/扮演 角色名` 让 AI 即时切换角色。**所有角色均须遵循苏格拉底式对话的基本原则**（追问、引导、不直接给答案），只是在语气和关注点上有所区别：<br> • **苏格拉底**（默认）：中性、追问、引导<br> • **热情读者**：热情、联系现实案例<br> • **批判者**：挑战用户观点，寻找逻辑漏洞<br> • **作者**：以作者视角回应<br> • **领域专家**：引入跨学科知识 | 角色切换后，AI 首条回复中以角色身份自然回应 |
| F-017-A | 动态角色发现 | P2 | 在角色切换菜单底部，增加"**探索更多角色...**"按钮。点击后，内置"发现助手"AI 联网搜索并推荐 3-5 个新角色，用户可勾选临时或永久使用。 | 推荐角色与当前书籍主题相关，无低俗/违规角色 |
| F-018 | 模式切换 | P1 | `/模式 模式名` 切换问答风格：苏格拉底（默认）、费曼、批判性、联想等。**模式与角色可叠加使用**。 | 模式切换实时生效，无需重启对话 |
| F-019 | 对话控制命令 | P0 | `/总结`：生成当前视野摘要<br>`/重试`：换个方式提问<br>`/换个话题`：从新角度提问<br>`/重读`：引用并分析原文 | 所有命令识别准确率 ≥ 95% |

> **🔄 品牌一致性修正（F-017）**：原设计允许切换为"杠精"等角色，与产品"苏格拉底"品牌矛盾。修订为：所有角色都必须遵循苏格拉底式对话的核心原则（不直接给答案、通过提问引导思考），区别仅在于语气和侧重点。删除"杠精"角色，改为"批判者"（建设性地寻找逻辑漏洞）。

---

### 2.7 用户配置（含动态数据源发现）

| 功能ID | 功能名称 | 优先级 | 详细描述 | 验收标准 |
| :--- | :--- | :--- | :--- | :--- |
| F-020 | 导师人设配置 | P0 | 自定义：基础人设模板或自定义提示词、用户画像（背景、兴趣）、语言风格、**API Key**（密码框，本地加密存储）。 | API Key 加密存储，明文不可见；导入/导出配置时不包含 API Key |
| F-021 | 联网预热开关 | P1 | 开启/关闭外部数据源检索功能。 | 开关状态实时保存 |
| F-022 | 数据源配置 | P1 | 可勾选**豆瓣读书、知乎、搜索引擎**等数据源，拖动调整优先级排序。**仅展示已有合规接入方式的数据源**。 | 数据源列表可扩展，新增数据源需经过合规评审 |
| F-022-A | 动态数据源发现 | P2 | 在数据源配置面板，增加"**探索更多数据源...**"按钮。"发现助手"AI 联网搜索并推荐垂直社区或平台，用户可勾选临时或永久添加。**新增数据源需经过用户确认并展示数据使用说明**。 | 推荐的数据源有公开 API 或合规抓取方式 |

---

### 2.8 阅读标注与搜索（新增）

| 功能ID | 功能名称 | 优先级 | 详细描述 | 验收标准 |
| :--- | :--- | :--- | :--- | :--- |
| F-026 | 文本高亮标注 | P0 | 用户在阅读界面选中文字后，可选择高亮颜色（黄/绿/蓝/红）进行标注。所有标注自动保存，再次打开章节时恢复。**（V2.9规划）** | 标注保存成功率 100%；标注在原文位置准确 |
| F-026-A | 标注笔记 | P1 | 点击高亮区域可添加短评/笔记，与标注绑定存储。 | 笔记与标注同步保存 |
| F-026-B | 标注导出 | P1 | 支持将全书标注+笔记导出为 Markdown 或 JSON，格式与 F-030 对话精华导出保持一致。 | 导出内容完整，格式规范 |
| F-026-C | 标注与 AI 联动 | P1 | AI 在对话中可以引用用户的标注内容，形成"你之前标注了这段话，现在怎么看？"的联结式提问。 | AI 引用标注时明确说明引用来源 |
| F-027 | 章节内搜索 | P0 | 在阅读界面提供搜索框，支持关键词搜索，搜索结果高亮显示，支持上下跳转。**（V2.9规划）** | 搜索响应时间 ≤ 500ms（10万字章节内） |

> **📌 补充说明**：标注和搜索是阅读器的基础能力，原 SRS 遗漏了这两项功能，本次补充。**当前版本（V2.8）暂未实现，已列入V2.9优先级P0任务**。

---

### 2.9 数据持久化

| 功能ID | 功能名称 | 优先级 | 详细描述 | 验收标准 |
| :--- | :--- | :--- | :--- | :--- |
| F-023 | 阅读进度 | P0 | 记住每本书最后阅读的章节和位置。**每次翻页自动保存，防断电丢失**。 | 进度记录延迟 ≤ 3s；异常关闭后恢复进度误差 ≤ 2 页 |
| F-024 | 对话历史 | P0 | 按章节保存完整 Q&A 记录（含预热阶段），下次打开可继续。 | 对话历史保存完整，不丢失用户发言 |

---

### 2.10 章末回顾

| 功能ID | 功能名称 | 优先级 | 详细描述 | 验收标准 |
| :--- | :--- | :--- | :--- | :--- |
| F-028 | 章末回顾 | P1 | 当用户到达章节末尾或输入 `/本章结束` 时，AI 切换为"回顾"模式。 | 回顾模式与精读模式明确区分（UI 上有视觉提示） |
| F-028-A | 内容回忆 | P1 | AI 通过提问引导用户主动回忆核心论点、案例和细节。 | 提问基于本章实际内容，无幻觉 |
| F-028-B | 逻辑梳理 | P1 | AI 追问论证结构、推理跳跃点和说服力。 | |
| F-028-C | 知识联结 | P1 | AI 将本章内容与之前章节进行跨越式联结。 | 联结内容明确标注来源章节 |
| F-028-D | 下章预告 | P1 | 回顾结束后，AI 自然引入下一章，发起新预热或直接过渡。 | |

---

### 2.11 智能复习系统

| 功能ID | 功能名称 | 优先级 | 详细描述 | 验收标准 |
| :--- | :--- | :--- | :--- | :--- |
| F-029 | 智能复习系统 | P1 | 基于**改进的艾宾浩斯曲线**（结合用户主动反馈）的间隔复习机制。**用户可全局关闭复习提醒**。 | |
| F-029-A | 复习提醒 | P1 | 系统自动计算每个章节的复习时间窗口（如 1天、3天、7天、30天），在书架界面以**复习提醒徽章**显示。**提醒仅在应用内展示，不调用系统通知**。 | 提醒准确触发；不打扰用户 |
| F-029-B | 复习会话 | P1 | 点击复习提醒后，AI 开启精简复习对话：快速回忆 → 重点补漏 → 重新联结。**单次复习会话控制在 5-10 分钟内**。 | 复习会话时长合理 |
| F-029-C | 动态调整 | P1 | 用户每次复习后标记熟悉程度（掌握/有印象/需要重来），系统动态调整下一次复习间隔。**若用户连续两次标记"掌握"，该章节自动退出复习队列**。 | |
| F-029-D | 复习仪表盘 | P1 | 在侧边栏提供全局视图，显示所有书籍章节的记忆保持率和建议复习日期。 | |
| F-029-E | 本地计算 | P1 | 所有复习算法和进度数据存储于浏览器 IndexedDB，无需后端。**单本书复习数据估算：每章节约 5KB，100 章节的书约 0.5MB**。 | |

> **⚠️ 技术限制说明（F-029-E）**：IndexedDB 容量因浏览器而异（Chrome: ~80% 磁盘空间；Firefox: ~2GB；Safari: ~1GB）。若用户导入书籍总文本量超过 50MB，提示用户"当前书籍较多，建议定期导出并清理已读完的书籍数据"。

> **🔄 设计改进（F-029）**：原设计直接套用艾宾浩斯曲线，但该曲线基于"无意义音节"实验，对有意义的内容（如读书）效果有限。修订为：**结合艾宾浩斯曲线 + 用户主动反馈**决定复习间隔，而非强制推送。用户可随时关闭复习提醒。

---

### 2.12 对话精华导出

| 功能ID | 功能名称 | 优先级 | 详细描述 | 验收标准 |
| :--- | :--- | :--- | :--- | :--- |
| F-030 | 对话精华导出 | P1 | 在书籍菜单中提供"导出精华"按钮，AI 对话历史深度整理并生成文档。 | |
| F-030-A | AI 筛选逻辑 | P1 | AI 按优先级筛选：核心洞察、关键概念、转折性问题、薄弱环节、金句与类比。**导出前展示筛选结果预览，用户可手动调整**。 | 预览内容可编辑 |
| F-030-B | 导出格式 | P1 | 支持 **Markdown**（默认）、**纯文本**、**JSON**。 | |
| F-030-C | 导出结构模板 | P1 | 默认包含：书名与日期、核心观点、关键概念表、改变思考的问题、薄弱点、金句、阅读统计。 | |
| F-030-D | 用户自定义 | P1 | 导出前可勾选模块、选择时间范围、自定义文件名。 | |
| F-030-E | 隐私与性能 | P1 | 完全客户端完成，AI 整理后直接浏览器下载，不经过中间服务器。超长对话（> 500 轮）自动分段处理。 | |

---

### 2.13 隐私安全

| 功能ID | 功能名称 | 优先级 | 详细描述 | 验收标准 |
| :--- | :--- | :--- | :--- | :--- |
| F-025 | 本地存储策略 | P0 | 作为个人使用工具，**用户数据优先存储于本地**。联网功能（如预热）涉及将书籍元数据（书名、作者）发送至 AI API，**应在设置面板中明确告知用户哪些数据会被发送**。API Key 加密存储于本地，不上传任何服务器。**用户可在设置中查看"数据使用说明"，并随时关闭所有联网功能**。 | 设置面板中有"数据使用说明"入口，明确列出所有联网功能的数据发送范围 |

> **🔄 逻辑修正（F-025）**：原文档说"取消所有隐私边界的强制限制"，这是危险且矛盾的（后面又说 API Key 要加密）。修订为：**透明告知 + 本地优先 + 用户可控**。隐私不是"取消限制"，而是让用户清楚知道数据去向，并有权选择退出联网功能。

---

### 2.14 跨平台 PWA

| 功能ID | 功能名称 | 优先级 | 详细描述 | 验收标准 |
| :--- | :--- | :--- | :--- | :--- |
| F-031 | PWA 支持 | P1 | 启用 Service Worker，支持离线访问已解析章节和对话。可"添加到桌面"独立运行。 | Lighthouse PWA 评分 ≥ 80 |

---

## 3. 非功能性需求

- **性能**：
  - 章节切换后，文本提取 + AI 首问生成总等待时间 ≤ 5 秒（原 3 秒过于激进，修订为 5 秒）。
  - 文件大小上限：EPUB/TXT 50MB，PDF 100MB（扫描版 PDF 可能很大）。
  - AI 流式响应首字延迟 ≤ 1s。
- **国际化**：界面及 AI 对话语言从第一天起支持中英文。
- **可扩展性**：指令系统、角色库、模式库、数据源列表均预留扩展接口。
- **无障碍**：阅读界面支持字体缩放（≥ 200%）、深色模式、语音朗读（未来版本）。
- **浏览器兼容**：Chrome/Edge ≥ 90，Firefox ≥ 88，Safari ≥ 15。

---

## 4. 未来路线图 (V3+) 与扩展性设计指南

### 4.1 未来功能列表

| 功能 | 预计版本 | 描述 |
| :--- | :--- | :--- |
| 语音交互 | V3.0 | 基于 Web Speech API 的语音问答 |
| 知识卡片导出 | V3.0 | 对话要点一键存为卡片，导出到 Obsidian/Notion |
| 多人共读 | V3.5 | 生成只读链接，分享你与 AI 的对话历程 |
| 本地模型 | V3.5 | 接入 Ollama，完全离线运行 |
| 更多格式支持 | V3.0 | MOBI、AZW3 格式导入 |

### 4.2 V1 扩展性设计预留

为支持 V3+ 功能，V1 数据模型和设计应满足以下要求：

| 未来功能 | V1 需预留的设计 |
| :--- | :--- |
| 知识卡片导出到 Obsidian/Notion | 对话精华导出格式（F-030）应使用**结构化 Markdown**，每张卡片对应一个 `##` 标题块，便于程序化解析 |
| 多人共读 | 对话历史（F-024）应存储为**标准化 JSON Schema**，包含 `message_id`、`parent_id`、`timestamp` 字段，便于后续生成共享视图 |
| 本地模型（Ollama） | API 调用层（F-020）应抽象为 `LLMProvider` 接口，支持切换远端 API / 本地 Ollama，V1 实现时即采用此抽象，避免后续大规模重构 |
| MOBI/AZW3 支持 | 文件导入层（F-001）应采用**插件化解析器架构**，新增格式只需添加解析器插件，无需修改核心代码 |
| 语音交互 | V1 的 AI 对话接口应支持返回纯文本（无 Markdown 格式），便于后续接入 TTS |

---

## 5. 用户故事示例

> **用户打开《人类简史》**
>
> **AI**：在你翻开这本书之前，想先和你聊聊。
> 第一个问题："人类简史"这个书名，让你脑海中第一个浮现的画面是什么？
>
> **用户**：我会想到技术的进步，比如火、蒸汽机、互联网。
>
> **AI**：技术确实是书中非常重要的一条主线。但你有没有想过一个问题：**是技术改变了人类，还是人类因为某种独特的能力，才创造出了技术？**
> 这个问题，正是赫拉利在第一章就试图解答的。等你准备好进入正文时，输入 `/开始阅读`，我们一起去书里寻找线索。
>
> *(阅读完毕后)*
>
> **AI**：这一章你印象最深的概念是什么？三天后我会再问你一次，帮你把它变成长期记忆。（可在设置中关闭复习提醒）

---

## 6. 术语表

| 术语 | 定义 |
| :--- | :--- |
| 苏格拉底式对话 | 通过连续提问引导对方发现答案的对话方式，不直接给出结论 |
| 预热 | 正式阅读前，AI 通过提问帮助用户建立思维框架的过程 |
| 微评 | AI 对书籍生成的简短评语，展示在书架封面旁 |
| 标注 | 用户在阅读时对文字进行高亮或添加笔记的行为 |
| IndexedDB | 浏览器内置的 NoSQL 数据库，用于客户端数据存储 |
| LLMProvider | 大模型提供者抽象接口，统一远端 API 和本地模型的调用方式 |

---

## 7. V2.8 功能实现状态对照表

> **说明**：本表记录 V2.8 版本各功能的实际实现状态，便于追踪开发进度和 PR 审查。

### 7.1 已完整实现 ✅

| 功能ID | 功能名称 | 实现说明 | 代码位置 |
| :--- | :--- | :--- | :--- |
| F-001 ~ F-002 | 书籍导入与书架管理 | 支持 EPUB/PDF/TXT，元数据解析，网格/列表视图 | `packages/app/src/components/home/` |
| F-001-A ~ F-001-F | AI 微评引擎 | 3种类型（hook/question/resonance），缓存+持久化，forceRefresh | `packages/app/src/lib/book-mini-review.ts` |
| F-003 ~ F-004 | 章节解析 | EPUB/PDF 目录解析，文本提取 | Foliate.js 集成 |
| F-005 | 知识范围配置 | 当前章节/全书概要/作者背景/自定义知识库 | `packages/core/src/ai/socratic-prompt.ts` |
| F-006 | 打开即问 | 预热对话框，支持自动/智能/手动三种策略 | `packages/app/src/components/chat/PreheatingDialog.tsx` |
| F-007 | 联网预热 | 已集成 web-search 服务，支持搜索引擎检索 | `packages/app/src/lib/web-search.ts` |
| F-008 ~ F-011 | 预热对话流程 | 1-3个问题簇，联结引导，过渡到精读 | `packages/app/src/hooks/use-preheating.ts` |
| F-012 ~ F-016 | AI 精读对话 | 自动生成首问，评价+追问，流式响应，引用原文 | `packages/core/src/ai/use-socratic-chat.ts` |
| F-017 ~ F-019 | 角色切换与命令 | 多角色支持，命令解析系统 | `packages/core/src/ai/command-parser.ts` |
| F-020 ~ F-022 | 用户配置 | API Key 加密存储，数据源配置 | `packages/core/src/stores/settings-store.ts` |
| F-023 ~ F-024 | 数据持久化 | 阅读进度、对话历史 SQLite 存储 | `packages/core/src/db/` |
| F-025 | 隐私安全 | 本地优先，透明告知，用户可控 | 设置面板 "数据使用说明" |
| F-028 ~ F-029 | 章末回顾与复习 | 基础框架已搭建 | `packages/core/src/ai/socratic-dialogue-manager.ts` |
| F-030 | 对话精华导出 | Markdown/JSON 导出功能 | `packages/app/src/lib/conversation-export.ts` |
| F-031 | PWA 支持 | Service Worker 配置 | `packages/app/public/sw.js` |

### 7.2 部分实现 ⚠️

| 功能ID | 功能名称 | 实现状态 | 待完成内容 |
| :--- | :--- | :--- | :--- |
| F-001-B | 微评内容类型 | 3/4 种类型 | 第4种"作者轶事式"列入 V2.9 |
| F-008 | 开放性发问 | 已实现1-3问题簇 | 验收标准已调整为符合当前实现 |

### 7.3 未实现 ❌（V2.9 规划）

| 功能ID | 功能名称 | 优先级 | 计划版本 | 备注 |
| :--- | :--- | :--- | :--- | :--- |
| F-026 | 文本高亮标注 | P0 | V2.9 | 阅读器基础功能，需扩展 SelectionPopover |
| F-026-A | 标注笔记 | P1 | V2.9 | 依赖 F-026 |
| F-026-B | 标注导出 | P1 | V2.9 | 依赖 F-026 |
| F-026-C | 标注与 AI 联动 | P1 | V2.9 | 依赖 F-026 |
| F-027 | 章节内搜索 | P0 | V2.9 | 阅读器基础功能，可复用 Foliate.js 搜索能力 |

### 7.4 超规格实现 🚀

以下功能超出原 SRS 要求，为提升用户体验而额外实现：

| 功能 | 原 SRS 要求 | 实际实现 | 价值 |
| :--- | :--- | :--- | :--- |
| 预热策略 | 仅"打开即问" | 自动/智能/手动三种策略 | 用户可自主选择预热频率，智能策略避免重复打扰 |
| 微评缓存优化 | 异步生成 | 三层缓存（内存+Settings Store+SQLite）+ forceRefresh | 性能提升，支持强制刷新 |
| 数据库迁移系统 | 未明确要求 | 完整的 v1-v9 迁移脚本 | 确保 schema 演进平滑，无数据丢失 |
| 设置持久化 | 本地存储 | Zustand + withPersist + 文件系统备份 | 跨重启保持，异常恢复能力强 |
| 手动预热触发 | 仅 `/跳过预热` | ChatPanel 按钮 + 自定义事件通信 | 无弹窗刷新，体验流畅 |

---

**文档结束**
